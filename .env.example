# MongoDB connection (use service name when running in Docker)
MONGO_URI=mongodb://mongo:27017/reportheld

# Server
PORT=8000
NODE_ENV=development

# No secrets in Git. Override in .env (not committed) or environment.
